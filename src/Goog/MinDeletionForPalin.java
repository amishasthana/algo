package Goog;

public class MinDeletionForPalin {
    /* Same as max sunsequence
    // DP[i[[j] --> lomgest subseq so far
     DP[i][j] =  if (i == j) 2 + DP[i-1][j-1] , Max (DP[i+1][j], DP[i][j-1] )
    */
}
